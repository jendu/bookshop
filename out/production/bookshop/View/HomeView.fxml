<?xml version="1.0" encoding="UTF-8"?>

<?import java.net.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane fx:id="wrap" prefHeight="830" prefWidth="1200" styleClass="root" fx:controller="View.HomeViewController"
            xmlns="http://javafx.com/javafx/10.0.2-internal" xmlns:fx="http://javafx.com/fxml/1">
    <stylesheets>
        <URL value="@Style.css" />
    </stylesheets>

    <top>
        <HBox id="top" BorderPane.alignment="CENTER">
            <Label id="header" text="📚 Bookshop" />
        </HBox>
    </top>

    <center>
        <HBox fx:id="view">
            <VBox fx:id="left" id="left" prefWidth="150" BorderPane.alignment="CENTER">
                <Label id="leftText" text="Enter as:" />
                <Button id="customerBtn" text="Customer" onAction="#viewAsCustomer"/>
                <Button id="bookkeeperBtn" text="Bookkeeper" onAction="#viewAsBookkeeper" />
                <Button id="exitBtn" text="Exit" onAction="#exit" />
            </VBox>

            <GridPane fx:id="center"  BorderPane.alignment="CENTER">
                <columnConstraints>
                    <ColumnConstraints />
                </columnConstraints>
                <rowConstraints>
                    <RowConstraints />
                    <RowConstraints />
                </rowConstraints>
                <Label fx:id="title" id="title" text="📖 Welcome!" GridPane.rowIndex="0" />
                <Label id="content" text="Welcome to the Bookshop. Please enter either as a customer, or a bookkeeper."
                      GridPane.rowIndex="1" />
            </GridPane>
        </HBox>
    </center>

</BorderPane>
